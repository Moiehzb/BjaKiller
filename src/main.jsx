import React from 'react';
import { createRoot } from 'react-dom/client';
import EliteCounter from '../EliteCounter.jsx';

const root = createRoot(document.getElementById('root'));
root.render(<EliteCounter />);
